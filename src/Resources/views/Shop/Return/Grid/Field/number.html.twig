{% set order = rma_order_number_view(data) %}
{% if order is not null %}
    <a href="{{ path('sylius_shop_account_order_show', { number: order.number }) }}">#{{ order.number }}</a>
{% else %}
    #{{ data }}
{% endif %}
