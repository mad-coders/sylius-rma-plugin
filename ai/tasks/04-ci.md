# Task 04 - Update bitbucket-pipelines.yml

**Goal:** CI runs on the new platform.

## Steps
- `image: php:7.3` -> `php:8.2`
- `mysql:5.7` -> `mysql:8.0`
- Refresh apt extensions as needed for PHP 8.2.
- Keep the PHPUnit step; align commands with the local gates in AGENTS.md.
- Consider re-enabling the (currently commented) static-analysis step now that PHPStan/Psalm
  configs are migrated.

## Verify
- YAML is valid.
- Commands match the local quality gates.
